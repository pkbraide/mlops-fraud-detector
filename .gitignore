# Virtual environments
venv/
.venv/
env/

# Python bytecode / caches
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/

# Jupyter
.ipynb_checkpoints/

# Raw dataset cache written by fetch_openml (re-downloadable). Anchored to the
# repo root on purpose: app/static/data/ ships real presets and must be tracked.
/data/

# Environment / secrets
.env

# OS artefacts
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# NOTE: models/*.joblib is intentionally tracked so the API serves the real
# model on a fresh clone. Do not add it here.
